package com.example.learnerassignments.model;

/** The kinds of thing worth telling somebody about. Phase 6 adds the rest. */
public enum NotificationType {
    /** A facilitator released the marking for a session. */
    FEEDBACK_RELEASED
}
